---
interface Props {
  lang?: 'en' | 'es';
}

const { lang = "en" } = Astro.props;

const translations = {
  en: {
    title: "Need a Root Canal in Playa del Carmen? We’re Ready to Help",
    text1: "Tooth pain, infection, or swelling can be overwhelming—especially while traveling. Our clinic provides professional root canal treatment in Playa del Carmen for American patients and international visitors seeking fast, safe, and effective dental care in Mexico.",
    text2: "If you are searching for a root canal near me while visiting Playa del Carmen, our experienced team is prepared to diagnose the issue and relieve pain as quickly as possible."
  },
  es: {
    title: "¿Necesitas una Endodoncia en Playa del Carmen? Estamos Listos para Ayudarte",
    text1: "El dolor de muelas, la infección o la inflamación pueden ser abrumadores, especialmente cuando viajas. Nuestra clínica ofrece tratamiento de endodoncia profesional en Playa del Carmen para pacientes internacionales que buscan atención dental rápida, segura y eficaz en México.",
    text2: "Si estás buscando un especialista en endodoncia cerca de ti mientras visitas Playa del Carmen, nuestro equipo experimentado está preparado para diagnosticar el problema y aliviar el dolor lo antes posible."
  }
};

const t = translations[lang];
---

<section class="about-section">
  <div class="container">
    <div class="about__grid">
      <div class="about__image-container">
        <img src="/endodoncia.jpg" alt="Root Canal Specialist" class="about__img" />
      </div>

      <div class="about__content">
        <h2 class="about__title">{t.title}</h2>
        <div class="about__accent-line"></div>
        <p class="about__text about__text--highlight">
          {t.text1}
        </p>
        <p class="about__text">
          {t.text2}
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  :root {
    --gold: #A58E55;
    --black: #000000;
    --white: #ffffff;
  }

  .about-section {
    padding: 80px 0;
    background-color: var(--black); /* Fondo negro */
    color: var(--white); /* Letras blancas */
  }

  .container {
    margin: 0 auto;
    width: 85%; /* Estándar solicitado */
  }

  .about__grid {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 40px;
  }

  @media (min-width: 992px) {
    .about__grid {
      flex-direction: row; /* Desktop: Imagen izquierda, texto derecha */
      justify-content: space-between;
      gap: 60px;
    }
  }

  .about__image-container {
    flex: 1;
    width: 100%;
  }

  .about__img {
    width: 100%;
    height: auto;
    border-radius: 4px;
    border: 1px solid var(--gold); /* Borde dorado */
    object-fit: cover;
    box-shadow: 0 10px 30px rgba(0,0,0,0.5);
  }

  .about__content {
    flex: 1.2;
    text-align: left;
  }

  .about__title {
    font-size: 1.8rem;
    font-weight: 800;
    text-transform: uppercase;
    line-height: 1.2;
    margin-bottom: 20px;
    color: var(--white);
  }

  @media (min-width: 768px) {
    .about__title {
      font-size: 2.5rem;
    }
  }

  .about__accent-line {
    width: 80px;
    height: 3px;
    background-color: var(--gold); /* Línea en dorado */
    margin-bottom: 30px;
  }

  .about__text {
    font-size: 1rem;
    line-height: 1.7;
    margin-bottom: 20px;
    color: #e0e0e0; /* Gris muy claro para lectura suave en fondo negro */
  }

  .about__text--highlight {
    font-size: 1.15rem;
    font-weight: 600;
    color: var(--gold); /* Texto destacado en dorado */
  }

  @media (min-width: 768px) {
    .about__text {
      font-size: 1.1rem;
    }
    .about__text--highlight {
      font-size: 1.3rem;
    }
  }
</style>