---
interface Props {
  lang?: 'en' | 'es';
}

const { lang = "en" } = Astro.props;

const translations = {
  en: {
    title: "What Is Root Canal Treatment?",
    text1: "Root canal treatment, also known as endodontic treatment, is a procedure that removes infected or damaged tissue from inside the tooth. The goal is to eliminate pain, stop infection, and preserve the natural tooth.",
    text2: "This type of endodontic treatment in Playa del Carmen allows patients to avoid tooth extraction while restoring comfort and oral health."
  },
  es: {
    title: "¿Qué es el Tratamiento de Endodoncia?",
    text1: "El tratamiento de conducto, también conocido como tratamiento de endodoncia, es un procedimiento que elimina el tejido infectado o dañado del interior del diente. El objetivo es eliminar el dolor, detener la infección y preservar el diente natural.",
    text2: "Este tipo de tratamiento de endodoncia en Playa del Carmen permite a los pacientes evitar la extracción dental mientras restauran la comodidad y la salud bucal."
  }
};

const t = translations[lang];
---

<section class="what-is-section">
  <div class="what-is__bg-overlay"></div>
  
  <div class="container">
    <div class="what-is__grid">
      <div class="what-is__content">
        <h2 class="what-is__title">{t.title}</h2>
        <div class="what-is__accent-line"></div>
        <p class="what-is__text what-is__text--highlight">
          {t.text1}
        </p>
        <p class="what-is__text">
          {t.text2}
        </p>
      </div>

      <div class="what-is__image-container">
        <img src="/especialidad-about.avif" alt="Endodontic procedure" class="what-is__img" />
      </div>
    </div>
  </div>
</section>

<style>
  :root {
    --gold: #A58E55;
    --black: #000000;
    --white: #ffffff;
  }

  .what-is-section {
    position: relative; /* Necesario para el overlay de fondo */
    padding: 100px 0;
    background-color: var(--white);
    color: var(--black);
    overflow: hidden;
  }

  /* Estilo para la imagen de fondo difuminada */
  .what-is__bg-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url('/especialidad1.jpeg'); /* Imagen de fondo */
    background-size: cover;
    background-position: center;
    filter: blur(8px); /* Difuminado solicitado */
    opacity: 0.07; /* Transparencia muy sutil para que no resalte */
    z-index: 0;
    pointer-events: none;
  }

  .container {
    position: relative; /* Para quedar encima del fondo */
    z-index: 1;
    margin: 0 auto;
    width: 85%;
  }

  .what-is__grid {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 40px;
  }

  @media (min-width: 992px) {
    .what-is__grid {
      flex-direction: row;
      justify-content: space-between;
      gap: 60px;
    }
  }

  .what-is__content {
    flex: 1.2;
    text-align: left;
  }

  .what-is__title {
    font-size: 1.8rem;
    font-weight: 800;
    text-transform: uppercase;
    line-height: 1.2;
    margin-bottom: 20px;
    color: var(--black);
  }

  @media (min-width: 768px) {
    .what-is__title {
      font-size: 2.5rem;
    }
  }

  .what-is__accent-line {
    width: 80px;
    height: 3px;
    background-color: var(--gold);
    margin-bottom: 30px;
  }

  .what-is__text {
    font-size: 1rem;
    line-height: 1.7;
    margin-bottom: 20px;
    color: #111111;
  }

  .what-is__text--highlight {
    font-size: 1.15rem;
    font-weight: 600;
    border-left: 4px solid var(--gold);
    padding-left: 20px;
  }

  @media (min-width: 768px) {
    .what-is__text {
      font-size: 1.1rem;
    }
    .what-is__text--highlight {
      font-size: 1.3rem;
    }
  }

  .what-is__image-container {
    flex: 1;
    width: 100%;
  }

  .what-is__img {
    width: 100%;
    height: auto;
    border-radius: 4px;
    object-fit: cover;
    display: block;
    /* Se quitó la sombra dorada sólida (box-shadow) */
    box-shadow: 0 15px 35px rgba(0, 0, 0, 0.15); /* Sombra natural suave */
  }

  @media (min-width: 992px) {
    .what-is__img {
      max-height: 450px; /* Consistencia con la regla de 450px */
    }
  }
</style>